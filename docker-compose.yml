version: "3.9"
services:
 rabbitmq:
    image: "rabbitmq:3-management"
    ports:
      - "15672:15672"
    volumes:
      - 'rabbitmq_data:/data'
 fibonacci_rest:
    build: ./FibonacciRest/
    ports:
      - "80:80"
volumes:
  rabbitmq_data: